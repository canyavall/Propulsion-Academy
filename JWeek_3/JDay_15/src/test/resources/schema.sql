DROP TABLE DOCUMENT IF EXISTS;
CREATE TABLE DOCUMENT (ID INTEGER NOT NULL IDENTITY, NAME VARCHAR(75) NOT NULL, CREATIONDATE TIMESTAMP DEFAULT CURRENT_TIMESTAMP() NOT NULL, AUTHOR VARCHAR(50) NOT NULL, PRIMARY KEY (ID));